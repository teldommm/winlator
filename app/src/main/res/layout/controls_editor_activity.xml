<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/FLContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- InputControlsView (the drag/resize canvas) and the floating toolbar are both added
         programmatically in ControlsEditorActivity.onCreate(): the canvas via
         `new InputControlsView(this)`, the toolbar via ControlsEditorToolbarComposeHost.create()
         (a Compose replacement for the old native LinearLayout toolbar that used to be inlined
         here — see git history for its XML if ever needed for reference). -->
</FrameLayout>